@{
	ViewBag.Title = "Личная страница";
}

<div id="profile">
	<article id="profileTitul">
			 Личный кабинет
		<h2>@ViewBag.Msg</h2>
	</article>

	<nav>
		<article class="profileNavActive">
			@Ajax.ActionLink("Профиль", "PersonalPage",
						new AjaxOptions
						{
							UpdateTargetId = "profileTab"
						})
		</article>

		<article>
			@Ajax.ActionLink("Карточки", "ProfCardMenu",
						new AjaxOptions
						{
							UpdateTargetId = "profileTab"
						})
		</article>
	</nav>

	<div id="profileContent">
		<div id="profileTab">
			@Html.Action("PersonalPage")
		</div>
	</div>

</div>
